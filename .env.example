# Signal Logic Systems — local environment (sample)
# Copy this file to .env and fill in the real values. .env is gitignored.

# Customer HMAC secrets — one per customer, used by scripts/test-submission.js
# and by the Netlify Function in production. Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SLS_SECRET_BLUEASH=
